h1 = @task.name

p.state #{t(:task_state)}: #{{show_task_state(@task)}}
p.description #{t(:task_description)}: #{@task.description}

== link_to t(:edit), edit_task_path(@task), class: 'btn btn-primary'
==< link_to t(:back), profile_path, class: 'btn btn-default'
